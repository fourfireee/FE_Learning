# CLAUDE.md

## 项目是什么

个人前端学习笔记仓库。每个知识点一个 `docs/NN-*.md`，配套可运行示例放在 `examples/NN-*/`，总目录在 `知识.md`。

## 用户背景

- 背景：计算机图形学、native 渲染引擎与工具、shader 特效。前端是新领域。
- 目标：掌握前端基础概念与「分析/解决问题的思考方式」，以便在 AI 辅助开发前端项目时具备判断力、保持项目可控。
- 实际项目方向：① 基于自研 native 渲染引擎的 web 特效工具；② 类 ComfyUI 的 AIGC workflow 可视化编排工具。

## 回答风格

- 越简洁扼要越好，结论优先，避免冗长铺陈和重复。
- 用通俗易懂的语言解释概念和原理；可用类比。
- 涉及 native/图形/渲染时，可借用用户已有的图形学知识做类比。

## 文档写作约定（写 docs/ 时遵守）

- 用尽量通俗易懂的语言，精简描述概念和计算原理。
- 必要时提供文档内 code snippet 或 mermaid 流程图；更完整的示例放独立的 `examples/NN-*/` folder。
- 代码关键点加注释，注释用同样通俗的行文风格说明。
- 排版简洁：除标题外用 bullet points，不随意加粗或斜体。
- 英文缩写首次出现时给全称 + 中文，如 `URL（Uniform Resource Locator，统一资源定位符）`。
- 每个主要知识点一个独立 md，并在 `知识.md` 中引用。

## "comment" 工作流

当用户选中一段代码/文本并说 "comment"，就用简洁通俗的语言对其补充解释，并写进对应文件（沿用上面的注释风格）。
